name: ReviewTwits Get Test

on:
  push:
    branches:
      - feature/WhalesBob

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Get Path Information
        run: |
          cd /
          pwd
          ls -al
